import { EmailService } from '../src/common/utils/email.service';

describe('EmailService Import Test', () => {
    it('should import EmailService', () => {
        expect(EmailService).toBeDefined();
    });
});
