import { TokenBlacklistService } from '../src/common/utils/tokenBlacklist.service';

describe('TokenBlacklistService Import Test', () => {
    it('should import TokenBlacklistService', () => {
        expect(TokenBlacklistService).toBeDefined();
    });
});
